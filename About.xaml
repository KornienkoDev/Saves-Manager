<Window x:Class="SavesManager.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p = "clr-namespace:SavesManager.Properties"
        xmlns:local="clr-namespace:SavesManager"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.about}" Height="320" Width="280">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"></RowDefinition>
            
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            

            <RowDefinition Height="10"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Static p:Resources.saves_manager }" Grid.Row="2" Grid.Column="1" FontSize="18" HorizontalAlignment="Center"></TextBlock>

        <TextBlock Text="{x:Static p:Resources.about_text}" Grid.Row="3" Grid.Column="1" FontSize="14" TextWrapping="Wrap" Margin="0,10,0,0" ></TextBlock>
    </Grid>
</Window>
